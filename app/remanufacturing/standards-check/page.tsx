import StandardsComparator from "../standards/StandardsComparator";

// Legacy route kept so previously shared links open the current standards tool.
export default function StandardsCheckPage() {
  return <StandardsComparator />;
}
